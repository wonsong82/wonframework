public $id;
public $logged;
public $username;
public $name;
public $email;
public $active;
public $banned;
public $created_time;
public $last_login;	
public $groups;

authenticate():void // authenticate user and get public valus
login_form():void // display login form in html format
logout():void // log out the user
member_of($group):true or false // check if the user is member of $group
write_log($action, $value):void // write action logs
add_user($username, $password, $name, $email, $active=true, $banned=false):true or false $this->error // add a user
get_user_by_id($userid) : user array or false // get a user by id
get_user_by_username($username) : user array or false // get user by username
add_group($groupname, $editable=true) : true or false $error // add a new group
add_user_to_group($username, $groupname):true or false $error / add user to group






User Table
`id` SERIAL NOT NULL,
`username` VARCHAR(255) NOT NULL,
`password` VARCHAR(255) NOT NULL,
`name` VARCHAR(255) NOT NULL,
`email` VARCHAR(255) NOT NULL,
`active` INT(1) DEFAULT 1,
`banned` INT(1) DEFAULT 0,
`created_time` DATETIME NOT NULL,			
PRIMARY KEY(`id`)

Group Table
`id` SERIAL NOT NULL,
`name` VARCHAR(255) NOT NULL,
`editable` INT(1) DEFAULT 1,
`sort_order` BIGINT NOT NULL,
PRIMARY KEY(`id`)

Group Membership
`id` SERIAL NOT NULL,
`group_id` BIGINT NOT NULL,
`user_id` BIGINT NOT NULL,
PRIMARY KEY(`id`)

Log Table
CREATE TABLE IF NOT EXISTS `{$this->log_table}` (
`id` SERIAL NOT NULL,
`action` VARCHAR(255),
`value` VARCHAR(255),
PRIMARY KEY(`id`)